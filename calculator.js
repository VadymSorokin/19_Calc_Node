const set = require('./core/setNumber');
const add = require('./core/add');
const sub = require('./core/substract');
const div = require('./core/divide');
const mult = require('./core/multiply');

module.exports = {
	set,
	add,
	sub,
	div,
	mult,
}